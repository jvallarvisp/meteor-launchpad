machine:
  services:
    - docker

dependencies:
  override:
    - echo "No deps"

test:
  override:
    - echo "No tests yet..."

deployment:
  hub:
    branch: master
    owner: jvallarvisp
    commands:
      - ./ci-build.sh
